import fs from "fs"; const FILE = "./levels.json"; export function loadLevels() { try { const data = fs.readFileSync(FILE, "utf8"); return JSON.parse(data); } catch { return {}; } } export function saveLevel(locKey, level, levelAt) { const data = loadLevels(); data[locKey] = { level, levelAt }; fs.writeFileSync(FILE, JSON.stringify(data, null, 2)); }